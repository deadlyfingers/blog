{
  "name": "blog",
  "description": "developer blog",
  "version": "0.0.1",
  "license": "MIT",
  "preview": true,
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.3.1",
    "foundation-sites": "^6.5.0-rc.3"
  },
  "devDependencies": {
    "gulp": "github:gulpjs/gulp#4.0",
    "gulp-gh-pages": "^0.5.4"
  },
  "scripts": {
    "dev": "browser-sync start --server '_dev' --files '_dev' --extensions 'html' --port 3000 --no-ui"
  }
}
